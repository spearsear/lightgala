var express = require("express");
var router = express.Router();

//http://localhost:3000/decor
/*router.get('/',function(req,res){
    //render from ejs view under view dir
    res.render('xmasdecor.html',{decor_id: 0});
})*/

/*router.get('/:decor_id',function(req,res){
    //render from ejs view under view dir
    res.render('xmasdecor.html',{decor_id: req.params.decor_id});
})*/

module.exports = router;
